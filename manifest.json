{
  "manifest_version": 3,
  "name": "Element Locator Picker",
  "version": "1.0.0",
  "description": "Pick elements and get XPath, CSS, Playwright, Cypress, and Selenium locators. Activate with keyboard shortcut.",
  "permissions": ["activeTab", "scripting"],
  "background": { "service_worker": "background.js" },
  "action": {
    "default_popup": "popup.html",
    "default_title": "Element Locator Picker"
  },
  "commands": {
    "toggle-picker": {
      "suggested_key": { "default": "Ctrl+Shift+L", "mac": "Command+Shift+L" },
      "description": "Toggle element locator picker on the current page"
    }
  }
}
